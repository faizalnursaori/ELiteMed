import { TeamSection } from "@/components/TeamSection";

export const metadata = {
  title: "Teams | Elite Med",
};

export default function Teams() {
  return (
    <div>
      <TeamSection />
    </div>
  );
}
